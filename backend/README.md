# A6CARS - Backend

## Setup
1. Copy `.env.example` to `.env` and fill DB credentials.
2. Create the database: `mysql -u root -p < schema.sql`
3. Install packages: `npm install`
4. Start server: `npm run start` (or `npm run dev` with nodemon)

API base: `http://localhost:4000/api`